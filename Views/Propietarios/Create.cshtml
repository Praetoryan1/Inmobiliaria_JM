@model Propietario

@{
    ViewData["Title"] = "Nuevo propietario";
}

<div class="row justify-content-center">
    <div class="col-xl-9">
        <div class="mb-4">
            <h1 class="h2 mb-1">Nuevo propietario</h1>
            <p class="text-secondary mb-0">Complete los datos para registrar un propietario.</p>
        </div>

        <div class="card app-card">
            <div class="card-body p-4">
                <form asp-action="Create" method="post">
                    <partial name="_Formulario" model="Model" />
                    <div class="d-flex justify-content-end gap-2 mt-4">
                        <a asp-action="Index" class="btn btn-outline-secondary">Cancelar</a>
                        <button type="submit" class="btn btn-primary">Guardar propietario</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
