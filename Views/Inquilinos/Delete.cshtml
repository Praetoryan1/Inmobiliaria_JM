@model Inquilino

@{
    ViewData["Title"] = "Eliminar inquilino";
}

<div class="row justify-content-center">
    <div class="col-lg-7">
        <div class="card app-card border-danger">
            <div class="card-body p-4">
                <h1 class="h3 text-danger">Eliminar inquilino</h1>
                <p>Esta acción eliminará definitivamente el siguiente registro:</p>

                <div class="bg-light rounded p-3 mb-4">
                    <div class="fw-semibold">@Model.Apellido, @Model.Nombre</div>
                    <div class="text-secondary">DNI @Model.Dni · @Model.Email</div>
                </div>

                <p class="fw-semibold">¿Desea continuar?</p>
                <form asp-action="Delete" asp-route-id="@Model.IdInquilino" method="post">
                    <input asp-for="IdInquilino" type="hidden" />
                    <div class="d-flex justify-content-end gap-2">
                        <a asp-action="Index" class="btn btn-outline-secondary">Cancelar</a>
                        <button type="submit" class="btn btn-danger">Sí, eliminar</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
