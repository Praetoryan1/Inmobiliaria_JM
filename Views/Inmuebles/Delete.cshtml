@model Inmueble

@{
    ViewData["Title"] = "Eliminar inmueble";
}

<div class="row justify-content-center">
    <div class="col-lg-7">
        <div class="card app-card border-danger">
            <div class="card-body p-4">
                <h1 class="h3 text-danger">Eliminar inmueble</h1>
                <p>Esta acción eliminará definitivamente la siguiente propiedad:</p>

                <div class="bg-light rounded p-3 mb-4">
                    <div class="fw-semibold">@Model.Direccion</div>
                    <div class="text-secondary">
                        @Model.TipoInmueble?.Nombre · @Model.Propietario?.Apellido, @Model.Propietario?.Nombre
                    </div>
                </div>

                <p class="fw-semibold">¿Desea continuar?</p>
                <form asp-action="Delete" asp-route-id="@Model.IdInmueble" method="post">
                    <input asp-for="IdInmueble" type="hidden" />
                    <div class="d-flex justify-content-end gap-2">
                        <a asp-action="Index" class="btn btn-outline-secondary">Cancelar</a>
                        <button type="submit" class="btn btn-danger">Sí, eliminar</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
